#!/bin/bash
set -e

echo "🔨 Rebuilding AllShared containers..."
docker-compose down
docker-compose build --no-cache
docker-compose up -d
echo "✅ Containers rebuilt and started!"
echo "📱 Visit: http://localhost:8888"
